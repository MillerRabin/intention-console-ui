const assert = window.chai.assert;

describe('Tasks', function () {
    it('#change state by name and params', async function () {
        const listener = window.document.getElementById('Listener');
        assert.ok(listener);
    });
});